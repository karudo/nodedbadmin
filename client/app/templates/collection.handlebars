{{pagination-links curPageNum=pageNum pageSize=pageSize allCount=model.allCount}}
<table class="table table-bordered table-striped">
<thead>
    <tr>
        {{#each col in model.headers}}
            <th>{{col}}</th>
        {{/each}}
    </tr>
</thead>
<tbody>
{{#each row in model}}
    <tr>
        {{#each col in row}}
            <td>{{col}}</td>
        {{/each}}
    </tr>
{{/each}}
</tbody>
</table>
