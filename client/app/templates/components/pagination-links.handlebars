{{#if pFirst}}
    {{link-to pFirst (query-params pageNum=pFirst sortBy=sortBy sortOrder=sortOrder)}}
{{/if}}
{{#if pPrev}}
    {{#link-to (query-params pageNum=pPrev sortBy=sortBy sortOrder=sortOrder)}}<i class="glyphicon glyphicon-arrow-left"></i>{{/link-to}}
{{/if}}
{{#each pages}}
    {{link-to pageNum (query-params pageNum=pageNum sortBy=sortBy sortOrder=sortOrder) class=active}}
{{/each}}
{{#if pNext}}
    {{#link-to (query-params pageNum=pNext sortBy=sortBy sortOrder=sortOrder)}}<i class="glyphicon glyphicon-arrow-right"></i>{{/link-to}}
{{/if}}
{{#if pLast}}
    {{link-to pLast (query-params pageNum=pLast sortBy=sortBy sortOrder=sortOrder)}}
{{/if}}
