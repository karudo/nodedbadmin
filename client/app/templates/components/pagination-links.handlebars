{{#if showPaginator}}
    <ul class="pagination pagination-sm">
        {{#if pPrev}}
            <li>{{#link-to (query-params pageNum=pPrev)}}&laquo;{{/link-to}}</li>
        {{/if}}
        {{#each pages}}
            <li {{bind-attr class="active"}}>{{link-to pageNum (query-params pageNum=pageNum)}}</li>
        {{/each}}
        {{#if pNext}}
            <li>{{#link-to (query-params pageNum=pNext)}}&raquo;{{/link-to}}</li>
        {{/if}}
    </ul>
{{/if}}