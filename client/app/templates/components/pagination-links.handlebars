{{#if pFirst}}
    {{link-to pFirst (query-params pageNum=pFirst sort=controller.sort)}}
{{/if}}
{{#if pPrev}}
    {{#link-to (query-params pageNum=pPrev sort=controller.sort)}}<i class="glyphicon glyphicon-arrow-left"></i>{{/link-to}}
{{/if}}
{{#each pages}}
    {{link-to pageNum (query-params pageNum=pageNum sort=controller.sort) class=active}}
{{/each}}
{{#if pNext}}
    {{#link-to (query-params pageNum=pNext sort=controller.sort)}}<i class="glyphicon glyphicon-arrow-right"></i>{{/link-to}}
{{/if}}
{{#if pLast}}
    {{link-to pLast (query-params pageNum=pLast sort=controller.sort)}}
{{/if}}
