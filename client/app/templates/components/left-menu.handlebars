{{#if connection}}
    {{#if dropDownItems}}
        {{select-drop-down curName="select database" handlerTagName="button" handlerClasses="btn btn-primary" items=dropDownItems action="setSelectedDD"}}
    {{/if}}
    {{#if listItems}}
        {{#each listItems}}
            <div>{{name}}</div>
        {{/each}}
    {{/if}}
{{/if}}